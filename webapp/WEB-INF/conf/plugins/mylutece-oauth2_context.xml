<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context-3.0.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

  <!-- All DataClient values are externalized in WEB-INF/conf/plugins/mylutece-oauth2.properties
       (keys mylutece-oauth2.dataclient.<name>.*). Comma separated lists for scopes and acrValues. -->

  <bean id="mylutece-oauth2.oauth2LuteceUserSessionService" class="fr.paris.lutece.plugins.mylutece.modules.oauth2.service.Oauth2LuteceUserSessionService"/>
  <bean id="mylutece-oauth2.authentication" class="fr.paris.lutece.plugins.mylutece.modules.oauth2.authentication.Oauth2Authentication"/>

  <bean id="mylutece-oauth2.authDataClient" class="fr.paris.lutece.plugins.mylutece.modules.oauth2.authentication.AuthDataClient">
        <property name="name" value="${mylutece-oauth2.dataclient.authData.name}" />
        <property name="default" value="${mylutece-oauth2.dataclient.authData.default}"/>
        <property name="dataServerUri" value="${mylutece-oauth2.dataclient.authData.dataServerUri}"/>
        <property name="tokenMethod" value="${mylutece-oauth2.dataclient.authData.tokenMethod}"/>
        <property name="scope" value="#{'${mylutece-oauth2.dataclient.authData.scopes}'.split('\s*,\s*')}"/>
        <!-- Optional eIDAS management : uncomment and set mylutece-oauth2.dataclient.authData.acrValues -->
        <!--
        <property name="acrValuesSet" value="#{'${mylutece-oauth2.dataclient.authData.acrValues}'.split('\s*,\s*')}"/>
        -->
    </bean>

  <!-- Alternative DataClient "authDataJson" (userinfo parsed as JSON). Disabled by default : to use it, uncomment this bean,
       the mylutece-oauth2.dataclient.authDataJson.* keys in mylutece-oauth2.properties, and call the callback with data_client=authDataJson.
  <bean id="mylutece-oauth2.authDataClientJson" class="fr.paris.lutece.plugins.mylutece.modules.oauth2.authentication.AuthDataClientJson">
        <property name="name" value="${mylutece-oauth2.dataclient.authDataJson.name}" />
        <property name="dataServerUri" value="${mylutece-oauth2.dataclient.authDataJson.dataServerUri}"/>
        <property name="tokenMethod" value="${mylutece-oauth2.dataclient.authDataJson.tokenMethod}"/>
        <property name="scope" value="#{'${mylutece-oauth2.dataclient.authDataJson.scopes}'.split('\s*,\s*')}"/>
        Optional eIDAS management : also add
        <property name="acrValuesSet" value="#{'${mylutece-oauth2.dataclient.authDataJson.acrValues}'.split('\s*,\s*')}"/>
    </bean>
  -->
</beans>
